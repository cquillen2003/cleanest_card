//For list view

//$("#new-row").after("<%= j(render "new_project_row") %>");
//$("#new-row").remove();

//For board view

$("#new-card").after("<%= j(render :partial => "project_card", :locals => { :project => @project, :cat_id => params[:category_id] }) %>");
$("#new-card").remove();