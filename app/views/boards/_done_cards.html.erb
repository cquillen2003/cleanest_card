<ul class="list-group" style="height: 250px" id="column-done">
	<% @done_cards.each do |card| %>			  
		<%= render :partial => "items/item_card", :locals => { :item => card, :cat_id => @kcat } %>				       
  	<% end %>
</ul>